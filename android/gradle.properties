org.gradle.jvmargs=-Xmx3G -XX:MaxMetaspaceSize=1G -XX:ReservedCodeCacheSize=256m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template. Set to false as a
# temporary workaround: purchases_flutter 10.10.0 still applies the legacy
# kotlin-android plugin, which is incompatible with built-in Kotlin under
# AGP 9 (see https://github.com/RevenueCat/purchases-flutter/issues/1759).
# Revert to true once RevenueCat ships a fix.
android.builtInKotlin=false
